---
name: ocr-transcription
description: >
  Orchestrates end-to-end OCR text extraction from PDF books and documents.
  Splits large PDFs, dispatches subagents for parallel transcription using
  view_file (no external OCR libraries), validates structural integrity,
  and merges final Markdown output. Use when user asks to "استخلص نص هذا الكتاب",
  "حوّل هذا PDF إلى نص", "نسّخ هذا المستند", "استخرج النص من هذا الملف",
  "extract text from PDF", "OCR this book", "transcribe this document",
  "معالجة كتاب", "تفريغ كتاب", or any request involving extracting, transcribing,
  or digitizing text from PDF files.
---

# مهارة استخلاص النصوص وتنسيق الأجزاء (OCR Orchestration & Transcription Skill)

- **المنشئ (Creator)**: نافع (مساعد مطور مناهج ومؤتمت تقني)
- **الملف المرجعي (Single Source of Truth)**: يعد this الملف هو المرجعية الكلية لكافة الوكلاء (الرئيسي والفرعيين).

---

## ١. استراتيجية المعالجة وإدارة الوكلاء (Orchestration & Processing)

- **دور الوكيل الرئيسي (Main Agent)**:
    - يتولى إدارة سير العمل، وقراءة ملفات الحالة، وإطلاق الوكلاء الفرعيين وتنسيقهم، وإجراء التحقق المؤجل والدمج النهائي.
    - **بروتوكول تعدد الملفات (Multi-file Protocol)**: إذا وجد الوكيل الرئيسي أكثر من ملف PDF في مجلد المصدر، يجب عليه تنبيه المستخدم في المحادثة وتخييره بين معالجة ملف واحد محدد أو معالجة جميع الملفات بالتتابع. فإن اختار معالجة الجميع، تولى الوكيل الرئيسي تشغيل الدورة الكاملة (تقسيم، استخلاص، تحقق، دمج) لكل ملف على حدة بالتوالي (واحداً تلو الآخر) دون خلط للملفات أو تداخلها.
- **منع المكتبات الخارجية (Hard Ban on External OCR/Text Extractors)**:
    - **يُمنع منعاً باتاً** استخدام أي مكتبات برمجية (مثل `pdfplumber` أو `PyMuPDF` أو `pypdf` أو `PyPDF2`) أو أدوات خارجية (مثل `tesseract` أو `pandoc`) لاستخلاص النصوص أو إجراء OCR.
    - **يجب حصرياً** الاعتماد على أداة `view_file` المدمجة في البيئة لقراءة ملفات PDF واستخلاص نصوصها وصورها مباشرة بواسطة قدرات النموذج الذاتية.
    - يُقتصر استخدام المكتبات القياسية في السكربتات المساعدة لمهام التقسيم الهيكلي لملفات PDF (`splitt_pdf.py`)، والتحقق من التنسيق (`validate_chunk.py`)، والدمج (`merge_parts.py`).
    - **تكامل أدوات التدقيق**: تم دمج منطق فحص التكرار (`extract_samples.py` سابقاً) بالكامل داخل سكربت الدمج `merge_parts.py` لتبسيط البنية، ويُحظر تشغيله كخطوة منفصلة.
- **سياسة تقسيم الملفات (Splitting Policy)**:
    - **حجم الجزء ثابت وحاسم (Hard Limit)**: 20 صفحة لكل جزء ولا استثناء. هذا الحد قرار معماري صارم لا يُعدّل بأي معامل اختياري.
    - **إذا كان** ملف PDF يزيد على 20 صفحة: يُشغل سكربت `scripts/splitt_pdf.py` لتقسيمه إلى أجزاء تحتوي 20 صفحة كحد أقصى.
    - **إذا كان** ملف PDF يحتوي 20 صفحة أو أقل: يُشغل سكربت `scripts/splitt_pdf.py` لتهيئة سير العمل وملف المتابعة `progress.json` دون إجراء تقسيم فعلي للملف.

---

## ٢. بروتوكول التنفيذ التتابعي (Sequential Execution Pipeline)

### الخطوة الأولى: التهيئة والتقسيم
1. يتحقق الوكيل الرئيسي من عدد ملفات PDF في مجلد المصدر. في حال التعدد، يستشير المستخدم تفاعلياً في المحادثة لتحديد طريقة المعالجة (ملف واحد أو الجميع).
2. تشغيل أمر التهيئة للملف النشط: `python scripts/splitt_pdf.py "pdf/input.pdf"`.
3. قراءة ملف المتابعة `output_parts/progress.json` لمعرفة نطاق الأجزاء والصفحات المطلوبة.

### الخطوة الثانية: تشغيل الوكلاء الفرعيين (الاستخلاص والكتابة فقط)
1. يحدد الوكيل الرئيسي الأجزاء ذات الحالة `"pending"` في ملف المتابعة، **ويستبعد** أي جزء له ملف مؤقت `output_parts/part_N_temp.md` متواجد في المجلد بالفعل، تفادياً لتكرار التشغيل عند الانقطاع.
2. يُطلق الوكلاء الفرعيون من نوع `"self"` بالتوازي في مجموعات (Batches) لا تتعدى **4 وكلاء** في الآن الواحد، تفادياً لتجاوز حدود معدلات الاستهلاك (API Quota Limits).
3. **رسالة الوكيل الفرعي (Subagent Prompt)**: يتضمن الوكيل الرئيسي في رسالته لكل وكيل فرعي: **مسار ملف PDF المطلوب معالجته**، **رقم الجزء (N)**، **نطاق الصفحات (من X إلى Y)**، **مسار ملف الإخراج `output_parts/part_N_temp.md`**، وتوجيه صريح بأن التوجيهات التفصيلية للنسخ موجودة في `references/transcription_rules.md`.
4. **يُمنع** الوكيل الفرعي من إجراء أي عمليات تحقق برمجية أو استدعاء لسكربت التحقق أو تعديل ملف المتابعة.

### الخطوة الثالثة: التحقق المؤجل (Deferred Validation)
1. **لا يُجرى** التحقق من أي جزء إلا بعد اكتمال عمل كافة الوكلاء الفرعيين وولادة جميع الملفات النصية المؤقتة.
2. يقوم الوكيل الرئيسي بتشغيل سكربت التحقق للأجزاء المطلوبة، ويدعم السكربت ثلاث صيغ:
   - **جزء واحد أو أجزاء متعددة**: `python scripts/validate_chunk.py 1 3 5`
   - **كافة الأجزاء ذات الملفات المؤقتة**: `python scripts/validate_chunk.py --all`
3. يقوم سكربت التحقق تلقائياً بتحديث حالة الأجزاء في ملف المتابعة `progress.json` إلى `"completed"` في حال النجاح مع توليد ملف المخرجات المعتمد، أو إلى `"failed"` في حال الإخفاق.

### الخطوة الرابعة: التعافي من الأخطاء (Error Recovery)
- إذا فشل جزء في التحقق (حالة `"failed"`):
  1. **يُحظر تماماً التجاوز التقديري للخطأ** أو افتراض خلو الصفحات دون التحقق الحسي منها.
  2. يقوم الوكيل الرئيسي بحذف ملف المخرجات المؤقت الفاشل للجزئية (`output_parts/part_N_temp.md`).
  3. يُعيد الوكيل الرئيسي إطلاق وكيل فرعي لاستخلاص الجزء بالكامل من البداية وكتابته في `output_parts/part_N_temp.md`، مع توجيهه لتلافي مواضع النقص أو الانقطاع.
  4. يُعاد تشغيل سكربت التحقق للجزء حتى يجتاز الفحص بنجاح وتتحول حالته في ملف المتابعة إلى `"completed"`.
- **بروتوكول فشل السكربت (Script Failure Freeze Protocol)**: إذا فشل تشغيل أي سكربت مساعد (`validate_chunk.py` أو `merge_parts.py` أو `splitt_pdf.py`) بخطأ بيئي (Python exception، رفض صلاحية، نفاد ذاكرة) وليس بسبب خطأ في محتوى النص، **يجمد الوكيل الرئيسي المهمة فوراً** ويوضح للمستخدم رسالة خطأ صريحة تتضمن اسم السكربت، رسالة الخطأ، ورقم الجزء المتأثر، وينتظر تدخل المستخدم.

### الخطوة الخامسة: الدمج والتدقيق النهائي
1. عند اكتمال كافة الأجزاء ووسمها بـ `"completed"`، يُشغل الوكيل الرئيسي سكربت الدمج:
   `python scripts/merge_parts.py`
   *(يقوم السكربت تلقائياً باستدعاء منطق فحص التكرار داخلياً قبل إجراء أي عملية مسح للبيانات).*
2. إذا اكتشف السكربت تكراراً للمحتوى، يُجهض عملية التنظيف ويُبقي الملفات الوسيطة في مجلد `output_parts` لتسهيل مراجعة الخلل ومعالجته، مع توليد تقرير التدقيق في `sampling_preview.txt`. ويخرج السكربت بكود خطأ غير صفري لتعليق العملية.
3. إذا اجتاز المستند الفحص بنجاح، يُتم السكربت تنظيف وحذف الملفات الوسيطة وملف المتابعة، ويُعلن تمام النجاح وتسليم المستند النهائي في مجلد `md/`.
4. **التحويل التلقائي لملف Word**: يقوم الوكيل الرئيسي تلقائياً بتشغيل سكربت التحويل فور انتهاء عملية الدمج والتدقيق بنجاح لكل ملف PDF:
   `python scripts/convert_to_docx.py "md/مسار_الملف.md" "word/مسار_الملف.docx"`
   يحوّل السكربت ملف Markdown النهائي (الموجود في مجلد `md/`) إلى مستند Word مستعيناً بمكتبة `python-docx` ويحفظه في مجلد `word/` بشكل تلقائي دون انتظار طلب من المستخدم، مع تطبيق التنسيقات والأنماط القياسية للعناوين والفقرات والقوائم.
5. **الانتقال للملف التالي**: إذا كان خيار المستخدم معالجة كافة الملفات، يعود الوكيل الرئيسي إلى الخطوة الأولى لتهيئة ومعالجة ملف PDF التالي في القائمة المجدولة دون توقف أو استئذان.

---

## ٣. قواعد وميثاق النسخ والتدقيق (Transcription Manifesto)

القواعد التفصيلية للنسخ والتدقيق (الأمانة اللفظية، التنسيق الهيكلي، خصوصية النصوص الشرعية، وصف الصور، وشرط إتمام بنية الجزء) مودعة في الملف المرجعي:
**[references/transcription_rules.md](references/transcription_rules.md)**
يجب على كل وكيل فرعي قراءة هذا الملف قبل الشروع في الاستخلاص.
