# حالة مشروع OCR Workflow

## البنية الحالية

- `_shared.py`: يحتوي على الأدوات والدوال المشتركة الموحدة (مثل write_json_atomic و parse_pages وتجزئة الأجزاء) للقضاء على تكرار الشيفرة.
- `splitt_pdf.py`: يجزئ صفحات PDF إلى أجزاء خامة، ويستورد دوال التحليل والكتابة الذرية من `_shared.py`.
- `validate_chunk.py`: يفحص موضع واصمات الصفحات وتسلسلها، ويحفظ الناتج والحالة ذرياً ويستورد دالة الكتابة الذرية من `_shared.py`.
- `merge_parts.py`: يثبت اكتمال الأجزاء، ويحصر الناتج داخل `md/`، ثم يدمج الأجزاء وينظف كافة وسائط ومجلدات الأجزاء الناجحة تلقائياً.
- `convert_to_docx.py`: يحول Markdown إلى Word بتنسيق الاتجاه العربي (RTL) عبر `pandoc`، ويكتب أولاً إلى ملف مؤقت فريد صحيح اللاحقة ثم يستبدله ذرياً.
- `pdf_to_images.py`: يرسم صفحات PDF إلى PNG مجزأة في `output_images/chunk_N/` عبر `PyMuPDF` بدقة **300 DPI** افتراضياً، ويستأنف من الحالة المطابقة فقط.
- `run_tests.py`: فحص سلوكي مستقل للتقسيم والتحقق والدمج ومسار الصور وتحويل Word.

## القرارات النافذة

- لا يبدأ تشغيل جديد وفي `output_parts/` أثر لتشغيل سابق.
- دليل العمل الحالي (`Cwd`) هو جذر المخرجات الافتراضية، ولا يغير ملف PDF الخارجي هذا الجذر.
- لا يُعد التحقق ناجحاً إذا فُقد ملف حالة أو جزء مطلوب.
- لا يطمس الدمج ملف Markdown نهائياً موجوداً، ولا يقبل اسماً يتجاوز `md/`.
- يشتق الدمج والتنظيف جذر التشغيل من موضع `output_parts/progress.json`، ولا يثق بحقل `project_root` داخل الحالة.
- يكون مسار الصور اختيارياً عند توافر `View File` وإلزامياً عند غيابها؛ ويُعلم المستخدم بالانتقال التلقائي دون سؤاله. ويستعمل المسار `PyMuPDF` بالدقة القياسية **300 DPI**، ويرفض حالة الاستئناف التالفة أو المخالفة.
- لا يفحص سكربت التقسيم وجود Pandoc؛ يتحقق سكربت التحويل منه عند بدء تحويل Word.
- يُحظر تشغيل الأوامر الاستكشافية بالطرفية (`--help`) أو قراءة الشيفرات المصدرية للسكربتات، مع الالتزام بالتوثيق المرجعي الملحق (`references/`).
- تُستخرج مسارات السكربتات المساعدة ديناميكياً باحتقاب المسار المطلق لمجلد المهارة الحاضن (`<Skill_Dir>`) لضمان العمل المستقل في البيئتين المحلية والعالمية (`~/.gemini/config/skills/`).

## التحقق

```powershell
$env:PYTHONIOENCODING="utf-8"; python -B run_tests.py
```

النتيجة الحالية: `20 passed, 0 failed`.
