# حالة مشروع OCR Workflow

## البنية الحالية

- `_shared.py`: يحتوي على الأدوات والدوال المشتركة الموحدة (مثل write_json_atomic و parse_pages) للقضاء على تكرار الشيفرة.
- `splitt_pdf.py`: يجزئ صفحات PDF إلى أجزاء خامة، ويستورد دوال التحليل والكتابة الذرية من `_shared.py`.
- `validate_chunk.py`: يفحص موضع واصمات الصفحات وتسلسلها، ويحفظ الناتج والحالة ذرياً ويستورد دالة الكتابة الذرية من `_shared.py`.
- `merge_parts.py`: يثبت اكتمال الأجزاء، ويحصر الناتج داخل `md/`، ثم يدمج الأجزاء وينظف وسائط التشغيل الناجح.
- `convert_to_docx.py`: يحول Markdown إلى Word عبر `pandoc` من غير تثبيت صامت أو تغيير للنظام.
- `pdf_to_images.py`: يرسم صفحات PDF إلى PNG مستقلة عبر `PyMuPDF` (المسماة `fitz`) لسرعة ودقة أعلى، ويستورد أدوات التحليل والكتابة الذرية من `_shared.py` مع معالجة مرنة للنطاقات.
- `run_tests.py`: اختبار سلوكي مستقل للتقسيم والتحقق والدمج والتحويل الرسومي المرن.

## القرارات النافذة

- لا يبدأ تشغيل جديد وفي `output_parts/` أثر لتشغيل سابق.
- لا يُعد التحقق ناجحاً إذا فُقد ملف حالة أو جزء مطلوب.
- لا يطمس الدمج ملف Markdown نهائياً موجوداً، ولا يقبل اسماً يتجاوز `md/`.
- مسار الصور اختياري مستقل، ويستعمل `PyMuPDF` للرسم الحقيقي، ويحفظ `.progress.json` بعد كل صفحة، ويتسامح مع مدخلات النطاقات الفاسدة والخارجة عن الحدود تلقائياً (Clamping).

## التحقق

```powershell
$env:PYTHONIOENCODING="utf-8"; python -B run_tests.py
```
