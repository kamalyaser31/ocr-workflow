# مسار عمل استخلاص النصوص ورقمنة الكتب (OCR Workflow)

نظام متكامل لإدارة وأتمتة عمليات استخلاص النصوص العربية والإنجليزية من ملفات PDF الممسوحة ضوئياً، مع الحفاظ على التنسيق الهيكلي وضمان سلامة النصوص الدينية والأدبية وفق معايير برمجية وتدقيقية صارمة.

تمت هيكلة هذا الدليل ليكون دليلاً شاملاً يدمج خطط الاستخدام اليومي للمستخدم البشري والوكلاء الأذكياء، مع شرح البنية البرمجية والمساهمة في السكربتات المساعدة.

---

## ١. معلومات المؤلف والتواصل (Author & Contact)

- **المؤلف**: كمال ياسر (Kamal Yaser)
- **البريد الإلكتروني**: [kamalyaser31@gmail.com](mailto:kamalyaser31@gmail.com)
- **تليجرام**: [@kamalyaser31](https://t.me/kamalyaser31)

---

## ٢. الهيكل التنظيمي للمستودع (Repository Structure)

يتكون المشروع من مجلدات تشغيل البيانات، ومجلد المهارة المستقل الذي يحتوي على الملفات والسكربتات البرمجية:

### أ. مجلدات تشغيل البيانات (Data & Execution Folders)
- **[pdf/](pdf/)**: المجلد المخصص لاستقبال ملفات PDF المصدر (توضع فيه الكتب المراد تفريغها).
- **[output_parts/](output_parts/)**: المجلد المؤقت الذي تودع فيه الـ chunks المقسمة والمستخلصة وسجل التقدم (`progress.json`).
- **[md/](md/)**: المجلد المخصص لاستلام ملفات Markdown (.md) النهائية الناتجة عن الدمج.
- **[word/](word/)**: المجلد المخصص لاستلام ملفات Word (.docx) النهائية الناتجة عن التحويل التلقائي.

### ب. مجلد المهارة القياسي المستقل (Skill Directory)
- **[.agents/skills/ocr-transcription/](.agents/skills/ocr-transcription/)**: يحتوي على كافة التوجيهات والسكربتات البرمجية لتشغيل المهارة:
  - **[SKILL.md](.agents/skills/ocr-transcription/SKILL.md)**: المرجعية المعمارية الكلية التي تضبط مسار العمل للوكلاء.
  - **[scripts/](.agents/skills/ocr-transcription/scripts/)**: السكربتات المساعدة لإدارة وتسيير مراحل العمل:
    - **[splitt_pdf.py](.agents/skills/ocr-transcription/scripts/splitt_pdf.py)**: تقسيم ملفات PDF الكبيرة إلى chunks (20 صفحة للـ chunk) وتوليد سجل المتابعة، مع دعم معالجة نطاق صفحات مخصصة.
    - **[validate_chunk.py](.agents/skills/ocr-transcription/scripts/validate_chunk.py)**: فحص الـ chunks المستخلصة والتأكد من مطابقتها الهيكلية للمعايير المعتمدة.
    - **[merge_parts.py](.agents/skills/ocr-transcription/scripts/merge_parts.py)**: دمج الـ chunks المعتمدة وتدقيقها ضد التكرار قبل إخراج الملف النهائي إلى مجلد `md/`.
    - **[convert_to_docx.py](.agents/skills/ocr-transcription/scripts/convert_to_docx.py)**: تحويل مخرجات Markdown النهائية إلى مستندات Word (.docx) منسقة بالاعتماد الحصري على Pandoc.
  - **[references/](.agents/skills/ocr-transcription/references/)**: مجلد الوثائق المرجعية للمهارة:
    - **[transcription_rules.md](.agents/skills/ocr-transcription/references/transcription_rules.md)**: ميثاق النسخ والتدقيق التفصيلي وقواعد التعامل مع النصوص والصور والتلف المادي وقواعد مناقلة الملفات.
    - **[execution_rules.md](.agents/skills/ocr-transcription/references/execution_rules.md)**: بروتوكول إدارة التشغيل والتعافي من أخطاء السكربتات وتجميد المهمة.

---

## ٣. دليل تشغيل واستخدام مسار العمل (Execution & Operation Guide)

### أ. المتطلبات الأساسية (Prerequisites)
- تثبيت بيئة بايثون (Python 3.x) مع مكتبة `pypdf` لإدارة وتقسيم الملفات.
- أداة `pandoc` مثبتة ومسجلة في PATH، وفي حال فقدانها سيقوم السكربت بمحاولة تثبيتها صامتاً عبر `winget` تلقائياً.

### ب. خطوات العمل التفصيلية (Step-by-Step)

#### الخطوة الأولى: التمهيد وتحديد الصفحات
ضع ملف الـ PDF المراد تفريغه في مجلد `pdf/` (على سبيل المثال: `pdf/input.pdf`).
شغل سكربت التقسيم لتحديد المسار وحالة العمل:
```bash
# خيار 1: تهيئة وتقسيم كامل مستند الـ PDF
python .agents/skills/ocr-transcription/scripts/splitt_pdf.py "pdf/input.pdf"

# خيار 2: تهيئة وتقسيم صفحات مخصصة فقط (متصلة أو متفرقة)
python .agents/skills/ocr-transcription/scripts/splitt_pdf.py "pdf/input.pdf" --pages "5,8,10-12"
```

*ملاحظة تقنية*: عند تشغيل خيار الصفحات المخصصة، يقوم السكربت تلقائياً بتجميع الصفحات المحددة في مجموعات متصلة وتجزئتها لـ chunks خامة مع إلحاق اسم المخرجات النهائية باللاحقة المخصصة (مثل `input_p5_8_10-12.md`) وتثبيت حقل `is_page_selection: true` في `progress.json`.

#### الخطوة الثانية: استدعاء الوكيل وتفويض المعالجة (AI Prompts)
وجه الذكاء الاصطناعي (الوكيل) للبدء في معالجة المستند باستخدام المهارة.

**أمثلة توجيهات الوكيل (Agent Prompting Examples):**
*   **مثال للملف الكامل (<= 20 صفحة) أو النطاقات الصغيرة المحددة**:
    > "قم بتفريغ ملف `pdf/input.pdf` لصفحات معينة `5,8,10-12` مستعيناً بمهارة `ocr-transcription` ومتبعاً قواعد النسخ بالكامل."
    *(سيسلك الوكيل تلقائياً المسار السريع Fast Track مستخلصاً النص ومحولاً إياه لوورد مباشرة)*
*   **مثال للملف الكامل (> 20 صفحة) أو النطاقات الكبيرة**:
    > "ابدأ بمعالجة كتاب `pdf/input.pdf` بالمسار القياسي للمهارة وقم بإطلاق الوكلاء الفرعيين لاستخلاص الأجزاء."

#### الخطوة الثالثة: تشغيل التحقق والدمج النهائي
بمجرد انتهاء الوكلاء من استخلاص النصوص وكتابتها في ملفات الأجزاء المؤقتة بـ `output_parts/`؛ يتم تشغيل التحقق ثم الدمج كالتالي:
```bash
# 1. تشغيل فحص التحقق الهيكلي لكافة الأجزاء
python .agents/skills/ocr-transcription/scripts/validate_chunk.py --all

# 2. تشغيل دمج الأجزاء المعتمدة وتطهير الملفات المؤقتة
python .agents/skills/ocr-transcription/scripts/merge_parts.py
```

*ملاحظة تقنية*: يقوم سكربت الدمج بكتابة الملف النهائي داخل مجلد `md/` افتراضياً. وإذا تبيّن أن حقل `is_page_selection` مفعل، يتخطى السكربت فحص التكرار (Duplication Check) صامتاً لتلافي الأخطاء الناتجة عن الفجوات النصية العمدية.

#### الخطوة الرابعة: التحويل إلى مستند Word
لتحويل مخرجات Markdown المعتمدة إلى مستند Word ذي محاذاة وتنسيق احترافي:
```bash
python .agents/skills/ocr-transcription/scripts/convert_to_docx.py "md/input.md" "word/output.docx"
```
*ملاحظة تقنية*: يقوم السكربت بإنشاء مجلد `word/` تلقائياً إن لم يكن متواجداً. وفي حال اكتشاف نصوص عربية داخل الملف، يتم حقن المعامل `-M dir=rtl` تلقائياً لضبط اتجاه الخطوط والجداول من اليمين إلى اليسار.

---

## ٤. البنية البرمجية وطرق المساهمة (Contributor Guide)

للراغبين في تعديل وتطوير السكربتات المساعدة؛ يرجى مراعاة الهيكلة الوظيفية الآتية:

*   **splitt_pdf.py**:
    *   يحتوي على دالة `parse_pages()` المسؤولة عن معالجة النطاقات واستخراج أرقام الصفحات الحقيقية (1-indexed).
    *   يحتوي على دالة `group_contiguous()` المسؤولة عن تجميع الصفحات في مجموعات متصلة لتجنب تحوير السكربتات الأخرى.
*   **convert_to_docx.py**:
    *   يعتمد حصرياً على أداة `pandoc` كخيار وحيد لضمان الدقة في الجداول والعناوين.
    *   يحتوي على منطق كشف وجود الأداة في PATH والمسارات الافتراضية للويندوز، مع تشغيل التثبيت التلقائي:
        `winget install -e --id JohnMacFarlane.Pandoc --silent --accept-source-agreements --accept-package-agreements`
    *   في حال الفشل، ينهار السكربت صراحة بكود خطأ غير صفري لتمكين الوكيل من إبلاغ المستخدم يدوياً.
*   **merge_parts.py**:
    *   يفحص حقل `is_page_selection` قبل استدعاء `extract_page_samples()`.
    *   يقوم بمسح كامل ملفات PDF المؤقتة للأجزاء وملف `progress.json` عند النجاح.

---

## ٥. ميثاق النسخ والتدقيق (Transcription Manifesto Summary)

تلتزم جميع عمليات الاستخلاص بملف القواعد الموحد **[transcription_rules.md](.agents/skills/ocr-transcription/references/transcription_rules.md)**، والذي ينص على:
*   **الأمانة المطلقة**: عدم تحوير المتن الأصلي للمستند وتخريج الأخطاء المطبعية في هوامش سفلية.
*   **الترقيم الفاصل**: تثبيت واصم الصفحات قياسياً: `--- Page [Number] ---`.
*   **الشرعيات والأدب**: إثبات الآيات بالرسم العثماني المحكم بين أقواس `﴿ ﴾` وفصل الشطرين الشعريين بـ `*`.
*   **مناقلة الملفات**: حظر إعادة توليد الملفات الطويلة وتوليدها كلياً لمجرد النسخ أو النقل؛ واستخدام أوامر نظام التشغيل المباشرة مثل `Move-Item` أو `Copy-Item` لترشيد استهلاك التوكنات.

---

## ٦. سجل التغييرات (Changelog)

### [v1.2] - معالجة النطاقات والاعتماد الحصري على Pandoc
- **معالجة الصفحات المحددة**: إضافة خيار `--pages` لمعالجة أجزاء معينة وحفظ الترقيم الأصلي وتسمية Windows آمنة.
- **الاعتماد المطلق على Pandoc**: إلغاء محرك التحويل القديم والاعتماد الكلي على Pandoc لدقة العناوين والجداول مع توفير التثبيت الذاتي الصامت والتعافي اليدوي.
- **توجيه مجلدات المخرجات**: تعديل مسار المخرجات الافتراضية ليكون في `md/` و`word/` تلقائياً للمحافظة على نظافة مجلد العمل.
- **تخطي فحص التكرار**: تعطيل فحص التكرار برمجياً عند تفعيل معالجة النطاقات المخصصة في الدمج.

### [v1.1] - التحديث المعماري والتوطين
- **توطين المهارة**: نقل الملف الإرشادي والسكربتات والمراجع إلى مجلد مهارة قياسي مستقل `.agents/skills/ocr-transcription/` محلياً داخل مجلد المشروع.
- **تأمين استقلالية المخرجات**: تعديل سكربت `convert_to_docx.py` لإنشاء مجلد الحفظ (مثل `word/`) تلقائياً وتفادي استثناءات الفقد.
- **عزل بروتوكولات الفشل**: إنشاء `execution_rules.md` وعزل تفاصيل بروتوكول تجميد العمل والتعافي فيه.

### [v1.0] - الإصدار الأول
- إطلاق النسخة الأولى من مسار عمل استخلاص النصوص وأتمتتها (OCR Workflow).
- توفير سكربتات التقسيم والتهيئة، والتحقق المؤجل، والدمج وفحص التكرار.
