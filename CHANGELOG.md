# سجل تغييرات مشروع ocr workflow (Changelog)

يرصد هذا السجل كافة التغييرات والتحسينات المنجزة في المشروع منذ الإصدار `v1.2` وحتى الإصدار الجديد `v1.3`.

---

## [Unreleased] - 2026-07-22

- **اعتماد الدقة القياسية 300 DPI لرسم الصور**: رفع القيمة الافتراضية للدقة `DEFAULT_DPI` في `pdf_to_images.py` إلى 300 DPI؛ لضمان أعلى وضوح وتنعيم للخط العربي والضبط بالحركات والتفاصيل التراثية عند تحويل صفحات PDF إلى صور.
- **تحديث المرجعيات وتثبيت القرارات المعمارية**: تحديث وثيقة `references/pdf_images_rules.md` لإقرار 300 DPI كمعيار قياسي، وتأكيد استقلال مكتبة `PyMuPDF` الحرة المحلية ورفض الاعتماد على المكتبات الخارجية (`pdf2image`) والامتناع عن التصدير بصيغة HTML صيانة لسعة الرموز واستقرار التنسيقات المخرجة عبر Markdown و Pandoc.

## [v1.3.1] - 2026-07-21

- **إصلاح تسرب ملفات الأجزاء المؤقتة**: تعديل `splitt_pdf.py` لفرض تفعيل راية التقسيم `is_split` عند استخدام نطاق صفحات مخصص، وتعديل `merge_parts.py` لتنظيف مجلد الأجزاء ومسح ملفات PDF المؤقتة وملف المتابعة دائماً بعد الدمج.
- **حل الفشل الكاذب عند الاستئناف**: تعديل `validate_chunk.py` لاستثناء الأجزاء المكتملة مسبقاً (التي حالتها `completed` في `progress.json` وملفها الناتج متواجد) من فحص الملفات المؤقتة الغائبة، مما يضمن نجاح التحقق بالرمز `0`.
- **التصدير الذري لملفات Word**: تعديل `convert_to_docx.py` ليكتب مخرجات Pandoc في ملف مؤقت بلاحقة `.tmp` أولاً، ثم استبداله بالملف النهائي عند النجاح التام، وحذف المؤقت عند الفشل لمنع قفل المحاولات التالية بملفات تالفة.
- **تحديث التبعيات والتوثيق الفعلي**: إضافة مكتبة `Pillow` صراحة إلى `requirements.txt` وتحديث `README.md` وسجل التغييرات ليتوافقا مع السلوك البرمجي الفعلي الجاري.
- **إضافة اختبارات نكوص معززة**: كتابة ثلاثة اختبارات نكوص شاملة في `run_tests.py` لتأكيد صحة إصلاحات التقسيم والاستئناف والتحويل الذري، ليرتفع عدد الفحوصات الناجحة إلى ٢٣ اختباراً.
- **مسار صور الصفحات عبر PyMuPDF (Fitz)**: إضافة سكربت `pdf_to_images.py` لتحويل صفحات PDF المحددة إلى صور مستقلة بصيغة PNG بدقة عالية، مع توفير نظام استئناف ذري عبر ملف `.progress.json` وتجاهل مجلد الصور الناتجة `output_images/` في Git.
- **حماية المسارات ومنع الاختراق (Path Traversal Guard)**: تحصين `pdf_to_images.py` ضد ثغرات اجتياز المسار بالتحقق من وقوع مسار الصورة المطلق ضمن المجلد المستهدف، واشتقاق الأسماء تلقائياً وتفادي قيم الحالة غير الموثوقة.
- **إصلاح معالجة الأخطاء وحماية التصدير**: تعديل `convert_to_docx.py` لمنع ابتلاع الاستثناءات العامة وتصعيد أخطاء القراءة في كاشف العربية، مع إدراج حماية تمنع طمس ملفات Word المصدرة مسبقاً.
- **تحسين زمن حفظ الحالة**: تعديل `splitt_pdf.py` لحفظ سجلات تقدم الأجزاء قبل البدء في معالجة الأجزاء لتلافي تلف البيانات عند الانقطاع.
- **تنظيف وهيكلة الأكواد**: تنسيق الأكواد بنسبة ١٠٠٪ باستخدام `black` وحل كافة اعتراضات `flake8` والـ E402 عبر استيراد `# noqa`.
- **حل الديون المعمارية وتطبيق مبدأ DRY**: استخلاص الدوال والوظائف المشتركة (مثل الكتابة الذرية للملفات `write_json_atomic` و`write_text_atomic` ومحرك تحليل نطاق الصفحات `parse_pages`) في وحدة مستقلة وموحدة `_shared.py` مع تعميم استخدامها على كافة السكربتات وإلغاء التكرار.
- **توسيع التغطية الاختبارية**: إضافة اختبارات سلوكية جديدة لسيناريوهات منع اجتياز المسار وحماية طمس الملفات المسبقة في `convert_to_docx.py` ليصل مجموع اختبارات التحقق الناجحة إلى ٢٠ اختباراً سلوكياً.

- **حاجز Plan Mode**: منع تشغيل السكربتات وإنشاء الملفات وإطلاق الوكلاء الفرعيين حتى يخرج المستخدم من نمط التخطيط، مع حظر توجيه الوكلاء إلى تجاوز تعليمات النظام.
- **توحيد سياسة Pandoc**: إلغاء التثبيت الصامت عبر `winget`، وتجميد التحويل عند غياب الأداة حتى يأذن المستخدم بتثبيتها ويتدخل بنفسه.
- **تحصين حالة التشغيل**: رفض طمس `output_parts/` المشغول، وكتابة ملفات الحالة والنواتج الوسيطة كتابة ذرية، وفشل التحقق عند أي جزء مفقود أو متجاوز.
- **دمج آمن مبسط**: منع الدمج الناقص واجتياز مسار `md/` وطمس الناتج السابق، وحذف فحص التكرار كاملاً.
- **اختبارات نكوص مستقلة**: إضافة `run_tests.py` لتغطية سيناريوهات حرجة في التقسيم والتحقق والدمج.

---

## [v1.3] - 2026-07-16

### 🚀 الميزات والتحسينات (Features & Enhancements)
- **تجزئة وتحليل ذكي للصفحات**: إضافة نظام آمن ومحكم لمعالجة نطاقات الصفحات المخصصة (`--pages`) مع التحقق من الحدود وإصدار تنبيهات تفصيلية للأرقام الخاطئة في سكربت التقسيم `splitt_pdf.py`.
- **دعم اللغة العربية والاتجاه من اليمين إلى اليسار (RTL)**: تمكين الكشف التلقائي عن النصوص العربية في سكربت التحويل لـ Word وتفعيل خيار التنسيق RTL تلقائياً عبر Pandoc.
- **تنسيق المستندات بقوالب مرجعية**: إضافة دعم لتنسيق ملفات docx الناتجة بالاعتماد على مستند مرجعي افتراضي لتوحيد السمت البصري.
- **سير عمل مدمج ومبسط**: تحديث آلية فحص التكرار (Duplication Check) وتخطي الفحص تلقائياً عند استخلاص صفحات مجزأة تلافياً للأخطاء الوهمية.
- **تأصيل حدود الوكلاء وقواعد السلوك**:
  - إضافة بند **"حظر قراءة السكربتات المساعدة"** في ملف المهارة `SKILL.md` لحفظ سعة السياق وترشيد استهلاك الرموز (Tokens).
  - إضافة بند **"حدود صلاحيات الوكلاء الفرعيين"** في قواعد التنفيذ `execution_rules.md` لمنع الوكلاء الفرعيين من تشغيل أوامر الطرفية، مما يحمي تدفق العمل من التجمد أو انتظار الموافقات.

### 🏗️ إعادة الهيكلة والتنظيف (Refactoring & Code Quality)
- **تنظيف وهيكلة السكربتات المساعدة**: إعادة هيكلة شاملة لسكربتات التقسيم والدمج والتحقق والتحويل للامتثال لـ PEP 8 وتحسين كفاءة معالجة الاستثناءات والمعاملات البرمجية.
- **تبسيط مسارات التشغيل**: إزالة بادئات المسارات الطويلة للسكربتات من ملف المهارة لتمكين التشغيل المباشر من جذر مستودع العمل دون تعقيد.

---
*تم توليد السجل وتوثيق الإصدار v1.3 تلقائياً بمهارة Git Diff Expert.*
